# Dogfood reports

Per-phase field-validation reports for cargoless. Snapshots of empirical
evidence at a point in time — distinct from the normative design docs in
`docs/` and the per-decision design docs in `docs/design/`.

- [`PHASE-2-REPORT.md`](PHASE-2-REPORT.md) — production hardening (Phase 2
  dogfood, May 2026). 12 findings against the v0 headless surface;
  8 fixed during the round, 1 launch-blocker resolved (anonymous OSS
  install path), 1 partial (D-A2 latency renegotiation pending operator
  decision), 1 design question.
