/target
**/*.rs.bk
Cargo.lock.orig
.DS_Store
# Local CAS / daemon scratch dirs (real paths come from config).
.tf-trunk-cache/
*.log
# Local-only MCP config carries a Plane API key. This repo is destined for
# open source — the key must NEVER be committed. Keep .mcp.json untracked.
.mcp.json
.env
# Local agent tooling (currently a bulk copy of tf-multiverse's .claude).
# Internal config + permission lists — must never enter this OSS-bound repo.
# The project contract lives in /CLAUDE.md (root) + docs/EXECUTION.md instead.
.claude/
.codex-worktrees/
